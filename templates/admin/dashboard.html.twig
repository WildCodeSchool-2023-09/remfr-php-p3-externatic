{% extends 'base.html.twig' %}

{% block title %}Dashboard administrateur
{% endblock %}

{% block body %}
	<div class="container text-center">
		<h1>Bienvenue sur votre dashboard admin!</h1>
		<div class="row">
			<div class="col dashboard-col">
				<div class="card shadow dashboard-card">
					<div class="card-body">
						<i class="bi bi-person-workspace"></i>
						<a href="{{ path('offer_index') }}" class="dashboard-link">Gestion des offres</a>
					</div>
				</div>
			</div>
			<div class="col dashboard-col">
				<div class="card shadow dashboard-card">
					<div class="card-body">
						<i class="bi bi-people-fill"></i>
						<a href="{{ path('user_candidates') }}" class="dashboard-link">Gestion des candidats</a>
					</div>
				</div>
			</div>
			<div class="col dashboard-col">
				<div class="card shadow dashboard-card">
					<div class="card-body">
						<i class="bi bi-building"></i>
						<a href="{{ path('company_index') }}" class="dashboard-link">Gestion des entreprises</a>
					</div>
				</div>
			</div>
		</div>
		<div class="row">
			<div class="col dashboard-col">
				<div class="card shadow dashboard-card">
					<div class="card-body">
						<i class="bi bi-person-vcard-fill"></i>
						<a href="{{ path('process_index') }}" class="dashboard-link">Gestion des process en cours</a>
					</div>
				</div>
			</div>
			<div class="col dashboard-col">
				<div class="card shadow dashboard-card">
					<div class="card-body">
						<i class="bi bi-person-circle"></i>
						<a href="{{ path('user_index') }}" class="dashboard-link">Gestion des utilisateurs</a>
					</div>
				</div>
			</div>
			<div class="col dashboard-col">
				<div class="card shadow dashboard-card">
					<div class="card-body">
						<i class="bi bi-box-arrow-right"></i>
						<a href="/logout" class="dashboard-link">Déconnexion</a>
					</div>
				</div>
			</div>
		</div>
	</div>
{% endblock %}
