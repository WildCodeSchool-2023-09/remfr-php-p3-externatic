<form method="post" action="{{ path('offer_delete', {'id': offer.id}) }}" onsubmit="return confirm('Etes-vous sûr de vouloir supprimer cette offre ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ offer.id) }}">
    <button class="btn btn-lg btn-primary submit-form">Supprimer l'offre</button>
</form>