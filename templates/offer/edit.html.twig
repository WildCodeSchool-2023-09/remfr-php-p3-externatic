{% extends 'base.html.twig' %}

{% block title %}Edit Offer
{% endblock %}

{% block body %}
<main class="main_home">
    <div class="container-sm form-container shadow offer-details">
        <h1 class="h1-form">Edition de l'offre {{ include('offer/_delete_form.html.twig') }}</h1>
        <div class="form-container form-new">
            {{ include('offer/_form.html.twig', {'button_label': 'Mise à jour'}) }}
        </div>
        <p class="text-center">
            <a href="{{ path('offer_index') }}" class="btn btn-link" style="margin-top:1rem" >Retour à la liste des offres</a>
        </p>
    </div>
</main>
{% endblock %}
