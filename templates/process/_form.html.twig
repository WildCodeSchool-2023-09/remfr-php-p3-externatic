{{ form_start(form) }}
    {{ form_widget(form) }}
    <p class="text-center">
        <button class="btn btn-primary mt-4" style="margin-top:1rem">{{ button_label|default('Enregistrer') }}</button>
    </p>
{{ form_end(form) }}
