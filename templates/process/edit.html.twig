{% extends 'base.html.twig' %}

{% block title %}Edit Process
{% endblock %}

{% block body %}
<main class="main_home">
    <div class="container-sm form-container shadow form-process"> 	    
        <h1 class="h1-form text-center mb-2 mt-3">Editer un process {{ include('process/_delete_form.html.twig') }}</h1>
        <div class="form-container form-new-process">
			{{ include('process/_form.html.twig', {'button_label': 'Update'}) }}
		</div>
        <p class="text-center">
			<a href="{{ path('process_index') }}">Retour à la liste des process</a>
		</p>
	</div>
</main>
{% endblock %}
