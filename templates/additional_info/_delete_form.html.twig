<form method="post" action="{{ path('app_additional_info_delete', {'id': additional_info.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ additional_info.id) }}">
    <button class="btn">Delete</button>
</form>
