{% extends 'base.html.twig' %}

{% block title %}AdditionalInfo{% endblock %}

{% block body %}
    <h1>AdditionalInfo</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ additional_info.id }}</td>
            </tr>
            <tr>
                <th>Birthdate</th>
                <td>{{ additional_info.birthdate ? additional_info.birthdate|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Nationality</th>
                <td>{{ additional_info.nationality }}</td>
            </tr>
            <tr>
                <th>Gender</th>
                <td>{{ additional_info.gender }}</td>
            </tr>
            <tr>
                <th>License</th>
                <td>{{ additional_info.license }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_additional_info_index') }}">back to list</a>

    <a href="{{ path('app_additional_info_edit', {'id': additional_info.id}) }}">edit</a>

    {{ include('additional_info/_delete_form.html.twig') }}
{% endblock %}
