{% extends 'base.html.twig' %}

{% block title %}Links{% endblock %}

{% block body %}
    <h1>Links</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ link.id }}</td>
            </tr>
            <tr>
                <th>Linkedin</th>
                <td>{{ link.linkedin }}</td>
            </tr>
            <tr>
                <th>Website</th>
                <td>{{ link.website }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_links_index') }}">back to list</a>

    <a href="{{ path('app_links_edit', {'id': link.id}) }}">edit</a>

    {{ include('links/_delete_form.html.twig') }}
{% endblock %}
