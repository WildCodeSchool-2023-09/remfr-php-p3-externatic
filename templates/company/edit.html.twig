{% extends 'base.html.twig' %}

{% block title %}Edit Company
{% endblock %}

{% block body %}
<main class="main_home">
    <div class="container-sm form-container shadow form-process">
		<h1 class="h1-form">Editer une entreprise {{ include('company/_delete_form.html.twig') }}</h1>
		{{ include('company/_form.html.twig', {'button_label': 'Enregistrer'}) }}
		<p class="text-center">
			<a href="{{ path('company_index') }}">Retour à la liste des entreprises</a>
        </p>
    </div>
</main>
{% endblock %}
